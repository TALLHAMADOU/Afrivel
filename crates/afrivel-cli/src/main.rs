//! Binaire `afrivel` — CLI globale du framework (scaffolding, codegen, dev, délégation runtime).
//!
//! Squelette (M0) — les sous-commandes (`new`, `make:*`, `dev`, …) arrivent en M3.

fn main() {
    println!("afrivel {} — CLI (squelette M0)", env!("CARGO_PKG_VERSION"));
}
